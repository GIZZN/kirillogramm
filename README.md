# Recipe Social Platform

Социальная платформа для обмена рецептами с функциями чата в реальном времени.

## Особенности

- 🔐 **Аутентификация пользователей**
- 🍽️ **Создание и обмен рецептами**  
- 💬 **Чат в реальном времени (SSE)** с поддержкой изображений
- ❤️ **Система лайков и избранного**
- 💬 **Комментарии к рецептам**
- 📱 **Адаптивный дизайн**
- 📷 **Отправка изображений в чате**

## Технологии

- **Frontend**: Next.js 15.5, React 19, TypeScript
- **Backend**: Next.js API Routes, PostgreSQL
- **Real-time**: Server-Sent Events (SSE)
- **Аутентификация**: JWT tokens
- **Стили**: CSS Modules

## Функции чата

### 📱 **Real-time сообщения:**
- Мгновенное отображение сообщений без перезагрузки
- Автоматическое переключение на чат с новым сообщением
- Автоматическая сортировка чатов по времени последнего сообщения
- Счетчики непрочитанных сообщений

### 📷 **Поддержка изображений:**
- Загрузка изображений до 5MB
- Поддержка всех основных форматов (JPG, PNG, GIF, WebP)
- Превью изображений в чате
- Клик для просмотра в полном размере

### ✨ **Smart UX:**
- Автоматический выбор первого чата при входе
- Автопрокрутка к новым сообщениям
- Отметки о прочтении сообщений
- Responsive дизайн для мобильных устройств

## Деплой на Vercel

### 1. Подготовка базы данных

Создайте PostgreSQL базу данных (можно использовать [Neon](https://neon.tech/) или [Supabase](https://supabase.com/)):

1. Создайте новую базу данных
2. Выполните SQL скрипты из папки `scripts/`:
   - `scripts/init-db.sql` - создание основных таблиц
   - `scripts/complete-db-setup.sql` - дополнительные таблицы и данные

### 2. Переменные окружения

В настройках проекта Vercel добавьте следующие переменные:

```env
# Database
DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password

# Authentication
NEXTAUTH_SECRET=your-secret-key-32-chars-min
NEXTAUTH_URL=https://your-domain.vercel.app
```

### 3. Деплой

1. Подключите репозиторий к Vercel
2. Vercel автоматически определит Next.js проект
3. Добавьте переменные окружения
4. Деплой произойдет автоматически

### 4. Проверка

После деплоя проверьте:
- Регистрация/авторизация работает
- Можно создавать рецепты     
- Чаты работают в реальном времени
- Загрузка изображений функционирует
- База данных подключена

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Создание .env.local с переменными окружения
cp .env.example .env.local

# Запуск в режиме разработки
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Получение профиля

### Рецепты       
- `GET /api/recipes` - Список рецептов
- `POST /api/recipes` - Создание рецепта
- `GET /api/recipes/[id]` - Рецепт по ID
- `POST /api/recipes/[id]/like` - Лайк рецепта

### Чаты
- `GET /api/chats` - Список чатов
- `POST /api/chats` - Создание чата
- `GET /api/chats/[id]/messages` - Сообщения чата
- `POST /api/messages/send` - Отправка текстового сообщения
- `POST /api/messages/upload-image` - Отправка изображения

### Real-time
- `GET /api/realtime` - SSE подключение для уведомлений

## Архитектура Real-time

Вместо WebSocket используется **Server-Sent Events (SSE)** для совместимости с Vercel:

### Как это работает:

1. **Подключение**: Клиент подключается к `/api/realtime`
2. **Отправка**: Пользователь отправляет сообщение через `/api/messages/send`
3. **Уведомления**: Сервер отправляет SSE уведомления всем участникам
4. **Автообновление**: Интерфейс обновляется автоматически
5. **Переподключение**: Автоматическое переподключение при разрыве

### Особенности:

- ✅ **Мгновенные уведомления** - сообщения появляются сразу
- ✅ **Автопереключение чатов** - при получении нового сообщения
- ✅ **Умная сортировка** - чаты сортируются по активности
- ✅ **Поддержка изображений** - через base64 в базе данных
- ✅ **Offline resilience** - переподключение при потере соединения

## Структура проекта

```
src/
├── app/
│   ├── api/                 # API routes
│   │   ├── auth/           # Аутентификация
│   │   ├── chats/          # Чаты
│   │   ├── messages/       # Сообщения и изображения
│   │   ├── recipes/        # Рецепты
│   │   └── realtime/       # SSE для real-time
│   ├── components/         # React компоненты
│   ├── context/           # React Context
│   ├── messages/          # Страница чатов
│   └── lib/               # Утилиты (db, auth)
└── scripts/               # SQL скрипты
```

## Производительность

- **Base64 изображения**: Изображения хранятся в БД для простоты деплоя
- **SSE вместо WebSocket**: Лучшая совместимость с Vercel
- **Автоматическая очистка**: Неактивные SSE соединения удаляются
- **Оптимизация запросов**: Эффективные SQL запросы с JOIN

## Безопасность

- **JWT токены** для аутентификации
- **Валидация файлов** - только изображения до 5MB
- **SQL параметры** - защита от SQL injection
- **CORS настройки** для SSE

## Лицензия

MIT